### Crawling
네이버 API 크롤링하는 프로젝트



Connect to URL & Search url = base + node + srcTExt

Receive Response as Json json.load()

getPostData()

json.dumps

python 함수설정
쿼리 선정
뉴스 서치
getPostDat()로 데이터를 받아오고 list 에 담아서 저장

list 형태의 데이터를 json 으로변환

input 형태로 서치할 요소 받기
getNaverSerarch로 서치
GetPostData()로 받기
json 으로 저장하기